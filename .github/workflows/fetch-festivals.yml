name: Fetch festivals

on:
  schedule:
    - cron: '0 20 * * 1'   # 매주 월요일 05:00 KST (분기 데이터라 주 1회면 충분)
  workflow_dispatch:
    inputs:
      reason:
        description: '실행 이유 (로그에만 기록, 선택)'
        required: false
        default: 'manual refresh'

permissions:
  contents: write

concurrency:
  group: fetch-festivals
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Show trigger context
        run: |
          echo "Event       : ${{ github.event_name }}"
          echo "Reason      : ${{ github.event.inputs.reason || '(scheduled)' }}"
          date -u "+UTC: %Y-%m-%d %H:%M:%S"
          TZ=Asia/Seoul date "+KST: %Y-%m-%d %H:%M:%S"

      - name: Fetch festival data
        env:
          FESTIVAL_API_KEY: ${{ secrets.FESTIVAL_API_KEY }}
        run: |
          if [ -z "$FESTIVAL_API_KEY" ]; then
            echo "::error::FESTIVAL_API_KEY secret is not set."
            exit 1
          fi
          node scripts/fetch-festivals.mjs

      - name: Check for changes
        id: diff
        run: |
          if git diff --quiet -- festivals/festivals.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "No changes — skipping commit."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            git --no-pager diff --stat festivals/festivals.json
          fi

      - name: Commit & push
        if: steps.diff.outputs.changed == 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          DATE_KST=$(TZ=Asia/Seoul date "+%Y-%m-%d")
          TRIGGER="${{ github.event_name }}"
          if [ "$TRIGGER" = "workflow_dispatch" ]; then
            MSG="chore(festivals): manual refresh ($DATE_KST)"
          else
            MSG="chore(festivals): weekly refresh ($DATE_KST)"
          fi

          git add festivals/festivals.json
          git commit -m "$MSG"
          git push

      - name: Summary
        if: always()
        run: |
          {
            echo "## Festival Fetch Summary"
            echo ""
            echo "- Trigger: \`${{ github.event_name }}\`"
            echo "- Changed: \`${{ steps.diff.outputs.changed || 'n/a' }}\`"
            echo "- KST: $(TZ=Asia/Seoul date '+%Y-%m-%d %H:%M:%S')"
          } >> "$GITHUB_STEP_SUMMARY"
