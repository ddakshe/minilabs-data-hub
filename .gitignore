.env*
!.env.example
node_modules/

# Python (scripts/stock-ipo)
__pycache__/
*.pyc

# 로컬 실행 로그 (LaunchAgent 가 쓰던 것 — 러너로 옮긴 뒤엔 안 쓰인다)
refresh-ott.log
local-jobs-status.json

# auto-option 가격표 PDF. 매번 새로 받으므로 저장하지 않는다 (75MB).
.pdf-cache/
.stock-tools/

# 서브에이전트 실행 스크래치 (커밋하지 않는다)
.superpowers/

# 튜닝 구간 backtest — 파라미터 적합에만 쓰고 화면에 안 나간다.
# walk-forward 로 언제든 재생성된다: node scripts/oil-backfill.mjs --tune
oil/tune/
